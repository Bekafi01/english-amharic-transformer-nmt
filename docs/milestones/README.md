# Milestones

One file per verified milestone: `phaseN_<name>.md`. Each records the command run, the config
used, the git commit, and the numbers observed. PLAN.md checkboxes are only ticked when a file
exists here.
